# Generated by 3AX-UI. Do not edit: this file is rewritten whenever the
# nginx settings are applied. Hand-written config belongs in another file.

map $ssl_preread_server_name $threeax_upstream {
    hostnames;

    # requests by IP address, no SNI
    "" 127.0.0.1:8082;

    # clients of edge-a → next hop
    www.neighbour.example 127.0.0.1:8084;

    default 127.0.0.1:8085;
}

server {
    listen 443;
    listen [::]:443;

    ssl_preread on;
    proxy_pass $threeax_upstream;
    proxy_protocol on;
    proxy_timeout 300s;
    proxy_connect_timeout 5s;
}

# clients of edge-a → next hop — raw stream: the PROXY header is taken off before it leaves the box
server {
    listen 127.0.0.1:8084 proxy_protocol;
    proxy_pass 203.0.113.9:443;
    proxy_timeout 300s;
    proxy_connect_timeout 5s;
}

# neighbour target of edge-a — raw stream: the PROXY header is taken off before it leaves the box
server {
    listen 127.0.0.1:8085 proxy_protocol;
    proxy_pass www.neighbour.example:443;
    proxy_timeout 300s;
    proxy_connect_timeout 5s;
}
